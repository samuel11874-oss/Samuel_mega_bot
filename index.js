// ... (mantenha as primeiras linhas iguais)
                    // TESTE: Removemos o filtro if (media > 9.5 && media <= 15.0)
                    // Agora ele captura qualquer média válida encontrada
                    if (media > 0 && media <= 20.0) { 
                        const chave = (match[1] + match[2]).toLowerCase().replace(/\s/g, '');
// ... (restante do código igual)
